{
  "name": "backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "echo 'backend: no build step (deployed as Lambda zip via CI, see infra/)'",
    "lint": "ruff check .",
    "test": "pytest",
    "typecheck": "mypy src",
    "migrate": "PATH=\"$PWD/.venv/bin:$PATH\" alembic upgrade head",
    "migrate:gen": "PATH=\"$PWD/.venv/bin:$PATH\" python scripts/new_migration.py"
  }
}
