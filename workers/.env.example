DATABASE_URL=postgresql+asyncpg://trading:trading@localhost:5432/trading
AWS_REGION=us-east-1
TRADE_INGEST_QUEUE_URL=
# Unset = real AWS. Set to http://localhost:4566 to point at LocalStack
# (see docker-compose.yml's `localstack` service and documentation/workers.md).
AWS_ENDPOINT_URL=
QUIVER_QUANT_API_KEY=
ENVIRONMENT=local
LOG_LEVEL=INFO
